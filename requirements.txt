# Dependencies have moved to pyproject.toml.
#
# For a dev environment (training + inference + test/lint tools):
#   pip install -e ".[dev]"
#
# For inference only (what wheel users get):
#   pip install -e .
#
# This file remains as a no-op to keep `pip install -r requirements.txt`
# from failing in any stale scripts. Delete when those are cleaned up.
